extends layout

block content
  h1 #{title}: #{bookinstance._id}
  p #{bookinstance.book.title}

  p #{bookinstance.imprint} - #{bookinstance.status} #{bookinstance.status === 'Available' ? '' : '(Due back: ' + bookinstance.due_back_formatted + ')'}
  
  p Do you really want to delete this Book Instance?
  
  form(method='POST' action='')
    div.form-group
      input#bookinstanceid.form-control(type='hidden',name='bookinstanceid', required='true', value=bookinstance._id )

    button.btn.btn-primary(type='submit') Delete